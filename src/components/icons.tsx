import {
  Loader2,
  LogOut,
  Settings,
  User,
  type LucideProps,
} from "lucide-react";

export type IconProps = LucideProps;

export const Icons = {
  spinner: Loader2,
  user: User,
  settings: Settings,
  logout: LogOut,
};
